{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "${user_config.node_command} \"${CLAUDE_PLUGIN_ROOT}/launcher.mjs\" prefetch"
          },
          {
            "type": "command",
            "command": "${user_config.node_command} \"${CLAUDE_PLUGIN_ROOT}/launcher.mjs\" hook session-start lumen --host claude"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Grep|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${user_config.node_command} \"${CLAUDE_PLUGIN_ROOT}/launcher.mjs\" hook pre-tool-use lumen"
          }
        ]
      }
    ]
  }
}
